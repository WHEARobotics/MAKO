[tool.robotpy]

# Version of robotpy this project depends on
robotpy_version = "2025.2.1"

# Which extra RobotPy components should be installed
# -> equivalent to `pip install robotpy[extra1, ...]
robotpy_extras = [
    # "all",
    "apriltag",
    "commands2",
    # "cscore",
    # "navx",
    # "pathplannerlib",
    # "phoenix5",
    "phoenix6",
    # "photonvision",
    # "playingwithfusion",
    "rev",
    # "romi",
    "sim",
    # "xrp",
]

requires = ["phoenix6==25.1.0", "robotpy-rev==2025.0.2"]
